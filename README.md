# CSV-analyzer
CSV Analyzer is a web application that allows users to upload CSV files, view them as a table, compute column-wise statistics, and generate histograms for numeric data. It is designed to handle datasets of up to 1000+ rows and automatically differentiates between numeric and categorical columns.
